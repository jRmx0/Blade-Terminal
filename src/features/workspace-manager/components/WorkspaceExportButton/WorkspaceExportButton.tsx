import MenuBarListButton from "../../../../components/MenuBarListButton/MenuBarListButton";

export default function WorkspaceExportButton() {
  const handleClick = () => {};

  return <MenuBarListButton label="Export..." onClick={handleClick} />;
}
